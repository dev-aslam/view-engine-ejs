<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <title>Items</title>
    <style>
        li.list-group-item.movie{
            display: flex;
        }

        li.list-group-item img{
            border: 2px solid grey;
            margin:0 1.5rem 0 0;
        }
    </style>
</head>
<body>

    <%- include('navbar') %>
    
    <div class="container">
        <h1>
            List of Movies
        </h1>
        <ul class="list-group">
            <% movies.forEach(movie => { %>
                <li class="list-group-item movie">
                    <img src="<%= movie.posterUrl %>" alt="<% movie.title %> Poster" width="200px" onerror="this.parentNode.style.display='none'">
                    <div class="movie-details">
                        <h2><%= movie.title %>(<%= movie.year %>)</h2>
                        <p><strong>Directors: <%= movie.director %></strong></p>
                        <p><strong>Actors: <%= movie.actors %></strong></p>
                        <p><strong>Plot: <%= movie.plot %></strong></p>
                    </div>
                </li>
            <% }); %>
        </ul>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>