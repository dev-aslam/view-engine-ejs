<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cards</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <style>
        .row .card{
            height: 100%;
        }
        .col-md-4{
            margin-bottom: 1rem;
        }

        .card img{
            height: 612px;
        }
    </style>

</head>
<body>
    <%- include('navbar') %>

    <div class="container">
        <h1>Card Movies</h1>
        <% for (let i = 0; i < movies.length; i += 3) { %>
            <div class="row">
                <% for (let j = i; j < Math.min(i + 3, movies.length); j++) { %>
                        <div class="col-md-4">
                            <div class="card">
                                <img src="<%= movies[j].posterUrl %>" alt="<%= movies[j].title %> Poster" onerror="<%= 'this.parentNode.parentNode.style.display=\'none\';' %>">
                                <div class="card-body">
                                    <h5 class="card-title"><%= movies[j].title %> (<%= movies[j].year %>)</h5>
                                    <p class="card-text"><strong>Directors:</strong> <%= movies[j].director %></p>
                                    <p class="card-text"><strong>Actors:</strong> <%= movies[j].actors %></p>
                                    <p class="card-text"><%= movies[j].plot %></p>
                                </div>
                            </div>
                        </div>
                <% } %>
            </div>
        <% } %>
    </div>

</body>
</html>
